# Projetos salvos

Os arquivos `.json` desta pasta são criados pelo botão **Salvar projeto** ou **Duplicar** quando o editor é executado com `npm start`.

Cada arquivo representa um projeto independente e é carregado automaticamente no `index`.

Para publicar esses projetos no GitHub remoto, faça commit/push dos novos JSONs normalmente.
